# Deprecated: use ci.yml. Kept so old PR links still resolve; forwards to the same gates.
name: Go

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Go workflow merged into CI — see .github/workflows/ci.yml"
